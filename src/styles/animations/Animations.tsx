import React from 'react'
import { keyframes } from 'styled-components';

export const MyAnimations = keyframes`
    
    /*  0%,100%{
        transform:scale(1);
    }
    50%{
        transform:scale(1.5);
    }  */

`; 

